{
  "id": "B7",
  "layer": "e2e",
  "title": "First load: how-to-play and Start on the same screen",
  "gate": "block",
  "steps": [{ "op": "openFresh" }],
  "expect": [
    { "assert": "visible", "testId": "start-screen" },
    { "assert": "visible", "testId": "start-panel" },
    { "assert": "visible", "testId": "howto" },
    { "assert": "count", "testId": "howto", "selector": "li", "value": 4 },
    { "assert": "visible", "testId": "start" },
    { "assert": "visible", "testId": "version" },
    { "assert": "heading", "name": "Classic Snake" },
    { "assert": "visible", "testId": "controls" },
    { "assert": "visible", "testId": "dpad" },
    { "assert": "below", "above": "howto", "below": "start" }
  ]
}
