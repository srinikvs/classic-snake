# Serve the promoted production build at repo root (index.html + assets/).
# Do not point this location at source files — /src/main.tsx is Vite-only.
location /classic-snake/ {
    try_files $uri $uri/ /classic-snake/index.html;
}
